import type { MessageCatalog } from './types'
import { CONFIG_WARNINGS_MESSAGE_KEYS } from './message-keys-config-warnings'

type ConfigWarningsMessageKey = (typeof CONFIG_WARNINGS_MESSAGE_KEYS)[number]

export const enConfigWarningsMessages = {
  "configWarnings.label.meaning": "What does this mean?",
  "configWarnings.label.importance": "Why does it matter?",
  "configWarnings.label.action": "What can I do?",
  "configWarnings.label.details": "Technical detail:",
  "configWarnings.title.startLoad": "A lot is read at startup",
  "configWarnings.meaning.startLoad": "Some rules and building blocks are loaded automatically every time the app starts.",
  "configWarnings.importance.startLoad": "The more that is loaded immediately, the faster the work can become noisy or slow.",
  "configWarnings.action.startLoadHigh": "Open the details and mark large always-loaded blocks for cleanup.",
  "configWarnings.action.startLoadObserve": "Just watch this if the app still responds well.",
  "configWarnings.title.heavyEntries": "Some building blocks are very large",
  "configWarnings.meaning.heavyEntries": "Some files bring a lot of text into the check at once.",
  "configWarnings.importance.heavyEntries": "Large files can push aside important hints and make results harder to understand.",
  "configWarnings.action.heavyEntries": "Open the details and split or move large blocks when you have a moment.",
  "configWarnings.title.missingDescriptions": "Some building blocks do not explain their purpose",
  "configWarnings.meaning.missingDescriptions": "The app is missing a short description for these entries.",
  "configWarnings.importance.missingDescriptions": "Without a description, it is hard to tell when a building block is needed.",
  "configWarnings.action.missingDescriptions": "Add a description or consciously leave the entry as-is if it is rarely used.",
  "configWarnings.title.fileHeader": "Check the file header",
  "configWarnings.meaning.fileHeader": "Some files contain header information the app cannot safely understand.",
  "configWarnings.importance.fileHeader": "That information may have no effect or may load a building block differently than expected.",
  "configWarnings.action.fileHeader": "Open the details and correct unknown header data or consciously leave it.",
  "configWarnings.title.ruleScope": "Rule probably loads too often",
  "configWarnings.meaning.ruleScope": "A rule is not clearly limited to the right files or situations.",
  "configWarnings.importance.ruleScope": "It may run even when it is not useful for the current work.",
  "configWarnings.action.ruleScope": "Review the rule details and add the right usage scope.",
  "configWarnings.title.alwaysRules": "Rules always run",
  "configWarnings.meaning.alwaysRules": "Some rules apply at every start instead of only for matching tasks.",
  "configWarnings.importance.alwaysRules": "That can be correct, but it can also make the app harder to follow.",
  "configWarnings.action.alwaysRules": "Only observe this if the rules are intentionally always active; otherwise narrow them.",
  "configWarnings.title.largeSources": "Large sources found",
  "configWarnings.meaning.largeSources": "Some entries contain a lot of text and should not always travel as one block.",
  "configWarnings.importance.largeSources": "Smaller blocks are easier to check, change, and find again.",
  "configWarnings.action.largeSources": "Open the details and mark large sources for later splitting.",
  "configWarnings.title.manySkills": "Many skill blocks active",
  "configWarnings.title.manyAgents": "Many agents active",
  "configWarnings.title.manyTeams": "Many teams active",
  "configWarnings.title.manyPlugins": "Many plugins active",
  "configWarnings.meaning.largeRoster": "The app finds more selectable building blocks than is helpful for a calm choice.",
  "configWarnings.importance.largeRoster": "Too many options at once make it harder to pick the right building block.",
  "configWarnings.action.largeRoster": "Later, bundle, move, or consciously leave building blocks you do not need.",
} as const satisfies Pick<MessageCatalog, ConfigWarningsMessageKey>
